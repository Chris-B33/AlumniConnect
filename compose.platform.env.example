# Optional: copy to compose.platform.env (gitignored locally) and run:
#   docker compose --env-file compose.platform.env --profile platform up -d
#
# Default values are baked into docker-compose.yml for zero-config local use.
# Override here if you need different credentials (do not commit real secrets).

# POSTGRES_USER=alumni
# POSTGRES_PASSWORD=alumni_dev_change_me
# POSTGRES_DB=alumni_connect

# MINIO_ROOT_USER=minioadmin
# MINIO_ROOT_PASSWORD=minioadmin
